// Write a function that returns the largest of the three numbers;

function maxOfThree(a,b,c) {
	return Math.max(a,b,c);
}

// or

const maxOfThree = function(a,b,c) {
	return Math.max(a,b,c);
}

// or

const maxOfThree = (a,b,c) => {
	return Math.max(a,b,c);
}

console.log(maxOfThree(500,50,5,5000);



